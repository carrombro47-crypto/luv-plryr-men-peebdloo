Flask==3.0.3
pymongo==4.8.0
requests==2.32.3
gunicorn==22.0.0
# Telegram MTProto upload — HTTP Bot API ke 50MB limit ko bypass karke
# ~2GB tak files upload karne ke liye (lecture videos 200-900MB hoti hain).
pyrogram==2.0.106
tgcrypto==1.2.5
