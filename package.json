{
  "name": "pw-live-proxy",
  "version": "2.0.0",
  "type": "module",
  "description": "HLS live stream proxy with full CORS support",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
